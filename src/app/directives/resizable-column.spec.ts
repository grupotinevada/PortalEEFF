import { ResizableColumn } from './resizable-column';

describe('ResizableColumn', () => {
  it('should create an instance', () => {
    const directive = new ResizableColumn();
    expect(directive).toBeTruthy();
  });
});
